/**
 * Mobile (< lg) hero visual band min-height. Keep in sync across:
 * - HeroSection (canvas/video column + dynamic loading shell)
 * - HeroCanvas (wrapper, Canvas, 2D fallback when WebGL off)
 * - HeroMobileHeroVideo
 */
export const MOBILE_HERO_CANVAS_MIN_H = "min-h-[92svh]" as const;
