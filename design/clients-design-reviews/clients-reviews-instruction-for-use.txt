# Client Reviews · Инструкция по использованию

## Что собираем
Блок отзывов про NEOCAR (forklifts / складская техника). Реализация —
`web/components/sections/TestimonialsSection.tsx`.

## ВАЖНО (исправление)
Старый текст файла описывал автомобильный сценарий (NeoCar = пригон авто).
Это НЕВЕРНО для текущего проекта. NEOCAR SRL — продажа, аренда и сервис
складской техники в Молдове. Используем сценарии ниже.

## Сценарии отзывов (3 шт. на каждый язык)
1) Логистика и производство — закрытие сезонного пика арендой электропогрузчиков.
2) Закупки/дистрибуция — срочные запчасти и РВД со склада NEOCAR.
3) Технический директор — многолетнее сервисное сопровождение Toyota/Mitsubishi.

## Тексты
- Источник истины — `web/messages/{ru,ro,en}.ts` → блок `Testimonials`
  (`title`, `subtitle`, `items[].name`, `items[].role`, `items[].quote`).

## Поведение
- 3 карточки в гриде на десктопе, стек на мобильном.
- Без аватаров (на старте) — только инициалы и текст.
